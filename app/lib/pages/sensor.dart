import 'package:flutter/material.dart';

class SensorLogPage extends StatelessWidget {
  const SensorLogPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Sensor Log',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
